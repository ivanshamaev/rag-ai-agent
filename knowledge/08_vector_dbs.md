# Векторные базы данных (Vector DB)

**Vector DB** — это специализированные системы для хранения эмбеддингов и выполнения быстрого поиска ближайших соседей (ANN). Они являются ключевым компонентом плотного (dense) поиска в RAG-системах и современных информационных приложениях.

---

## Принципы работы

Основная идея: каждый документ (или чанк текста) преобразуется в **плотный вектор** фиксированной размерности. Vector DB позволяет:

- **Эффективно хранить и индексировать векторы**, поддерживая миллионы и миллиарды элементов.
- Использовать структуры для поиска ближайших соседей:
  - **HNSW (Hierarchical Navigable Small World graph)** — графовая структура с отличным балансом точности и скорости.
  - **IVF (Inverted File Index)** — кластеризация векторов по кластерам/центроидам.
  - **PQ (Product Quantization)** — сжатие векторов для уменьшения памяти.
- Выполнять **Approximate Nearest Neighbors (ANN)**:
  - вместо поиска *точных* ближайших соседей применяется приближённый поиск;
  - выдаёт почти идентичные результаты, но на порядки быстрее.

ANN — это основной механизм масштабируемого dense retrieval: он позволяет искать по миллионам векторов за миллисекунды.

---

## Популярные решения

### **FAISS**
- Библиотека от Meta AI для высокопроизводительного поиска по эмбеддингам.
- Работает локально (CPU/GPU), интегрируется в Python/C++ проекты.
- Широкий набор индексов: Flat, IVF, HNSW, PQ, их комбинации.
- Часто используется внутри отдельных сервисов и офлайн-процессинга.

### **Milvus**
- Полноценная векторная база с возможностью горизонтального масштабирования.
- Поддерживает облачное и распределённое хранение.
- Имеет встроенный кластерный менеджмент, шардирование, бэкапы.
- Оптимальна для больших систем с миллионами/миллиардами объектов.

### **Qdrant**
- Производительная векторная БД с удобным API (REST/gRPC/SDK).
- Поддерживает фильтрацию по payload'ам, HNSW, sharding, репликацию.
- Легко разворачивается локально и в облаке.
- Отличный вариант для продакшена и быстрых прототипов.

---

## Особенности и выбор решения

### **Latency vs Recall**
Выбор индекса и его параметров определяет компромисс:

- **Recall↑** → более точные результаты, но медленнее (например, HNSW с высоким `efSearch`).  
- **Latency↓** → быстрее, но риск пропустить релевантные результаты.

Обычно выбирают параметры так, чтобы достигать **достаточного recall при минимальной задержке** (например, 95–98%).

---

### **Масштабирование**
Vector DB должны выдерживать большой поток запросов и рост данных:

- **Sharding** — распределение векторов по нодам.
- **Репликация** — поддержка отказоустойчивости.
- **Онлайн-обновление индекса** — добавление новых данных без полной перестройки.

---

### **Фильтры и метаданные**
Большинство Vector DB поддерживают:

- **Payload (metadata)** — любые структурированные данные: {title, date, tags, source}.  
- **Гибридные запросы**:
  - *«найти близкие по смыслу документы, но только в категории X»*  
  - *«ограничить поиск документами после определённой даты»*

Это делает Vector DB более универсальными по сравнению с чистым dense retrieval.

---

## Практические советы

- **Храните метаданные отдельно**, а payload используйте для фильтрации и последующего отображения результатов.  
- Для больших коллекций применяйте **HNSW** — он стабилен и предсказуем.  
- Используйте **PQ** или **IVF-PQ**, если ограничена память (например, при миллиардах векторов).  
- Планируйте периодические **реиндексации и анализ качества поиска**, особенно если обновления поступают часто.  
- Следите за **размерностью эмбеддингов**: слишком большие векторы → медленнее поиск и больше RAM.

---

## Пояснение для новичка: что это и как начать

Можно думать о Vector DB так:

- Это как «Google по смыслу»: вы задаёте запрос → он превращается в вектор → Vector DB ищет похожие векторы.  
- Каждый вектор — это *числовая копия текста*, которую легко сравнивать по смыслу.  
- Вместе с вектором хранится **payload** (метаданные), например:
  ```json
  {
    "text": "Как сварить кофе",
    "url": "example.com/coffee",
    "date": "2023-01-10"
  }

# Что такое HNSW, IVF и PQ, простыми словами?

HNSW — граф, по которому можно быстро «прыгать» между похожими точками.

IVF — делит пространство векторов на кластеры; сначала ищем нужный кластер, потом внутри него.

PQ — сжимает векторы, чтобы уменьшить память, немного теряя точность.

# Простой стартовый план для новичка

- Возьмите ~1000 документов.
- Разбейте на чанки и посчитайте эмбеддинги (sentence-transformers).
- Запустите Qdrant или FAISS локально.
- Загрузите векторы и метаданные.
- Выполните семантические запросы и сравните с обычным поиском.

Если вы видите, что система находит фрагменты, даже если вы переформулировали запрос — значит, ваш базовый RAG retrieval уже работает.

# Векторные базы данных (Vector DB)

**Vector DB** — специализированные хранилища для эмбеддингов, которые выполняют быстрый поиск ближайших соседей (ANN). Основной элемент RAG-систем, рекомендательных сервисов, поиска по смыслу.

---

# Индексы и алгоритмы ANN: HNSW, IVF, PQ — подробное сравнение

Чтобы эффективно искать по миллионам или миллиардам векторов, Vector DB используют разные структуры индексации. Ниже — расширенное сравнение трёх самых популярных методов.

## Таблица сравнения

| Характеристика | **HNSW** | **IVF** | **PQ / IVFPQ** |
|----------------|---------|---------|-----------------|
| Тип структуры | Граф | Кластеры (centroids) | Кодирование (квантование) |
| Скорость поиска | Очень высокая | Средняя/высокая | Очень высокая при больших объёмах |
| Точность (Recall) | Высокая | Средняя/высокая | Средняя (зависит от степени сжатия) |
| Память | Высокая | Средняя | Низкая |
| Обновления онлайн | Хорошо | Удовлетворительно | Ограничено |
| Идеален для | Высокоточной семантики | Баланса latency/recall | Огромных коллекций (10⁸–10⁹+) |

---

## Подробное объяснение

### 🧠 **HNSW (Hierarchical Navigable Small World)**

**Как работает:**  
Строится многоуровневый граф. Верхние уровни — «широкий обзор», нижние — «детальное уточнение». Алгоритм начинает с высоких уровней и постепенно спускается ближе к наиболее похожим точкам.  

**Плюсы:**
- Самый высокий recall среди ANN-методов.
- Отличное время отклика (обычно миллисекунды).
- Легко добавлять точки в реальном времени.

**Минусы:**
- Высокое потребление памяти.
- Более дорогое построение индекса.

**Когда использовать:**  
Когда приоритет — **точность поиска**, например юридические RAG, медицина, финансы.

---

### 🧩 **IVF (Inverted File Index)**

**Как работает:**  
Пространство делится на *n* кластеров (centroids).  
Поиск:  
1. Найти ближайшие к запросу центроиды.  
2. Искать среди векторов внутри выбранных кластеров.  

**Плюсы:**
- Быстрая навигация по кластерам.
- Удобно балансировать скорость/точность (через `nprobe` — число кластеров, куда смотрим).

**Минусы:**
- Ниже recall при одинаковой латентности, чем у HNSW.
- Требует подбора числа кластеров и параметров поиска.

**Когда использовать:**  
Когда важен **баланс скорости, памяти и качества**.

---

### 🧩 **PQ (Product Quantization)** и **IVF-PQ**

**Как работает:**  
Вектор разбивается на части, каждая часть квантуется (кодируется). Вместо хранения полного вектора хранится набор кодов (значительно меньше по памяти).

**Плюсы:**
- Очень низкое потребление памяти — подходит для миллиардов векторов.
- Высокая скорость поиска благодаря компактным кодам.

**Минусы:**
- Потеря точности из-за сжатия.
- Настройка PQ-кодов может быть сложной.

**Когда использовать:**  
Если данных **очень много**, и обычные методы не помещаются в RAM/GPU.

---

# Схема архитектуры Vector DB в RAG-системе

Ниже — типичная архитектура векторного поиска в составе RAG-пайплайна.

         ┌──────────────────────────────┐
         │         Пользователь          │
         └───────────────┬──────────────┘
                         │ Query
                         ▼
            ┌─────────────────────────┐
            │ Модель эмбеддингов      │
            │ (Encode query → vector) │
            └──────────────┬──────────┘
                           │ q-vector
                           ▼
           ┌─────────────────────────────────┐
           │           Vector DB              │
           │ (HNSW / IVF / PQ index + payload)│
           └─────┬───────────────────────────┘
                 │ top-k vectors + metadata
                 ▼
      ┌──────────────────────────┐
      │ Reranking (опционально)  │
      │ cross-encoder / LLM      │
      └──────────────┬──────────┘
                     │ ranked contexts
                     ▼
     ┌──────────────────────────────────────┐
     │ Модель генерации (LLM)               │
     │ answer ← generate(query, contexts)   │
     └──────────────────────────────────────┘


---

## Пояснение архитектуры

### 1) **Эмбеддер запроса**
Преобразует пользовательский запрос в вектор (sentence-transformers, OpenAI embeddings, BGE, Cohere и т.д.).

### 2) **Vector DB**
Содержит:
- **векторы** всех документов/чанков,
- **payload** (оригинальный текст, источник, дата),
- **индекс ANN** (HNSW/IVF/PQ),
- опционально — фильтры по метаданным.

Vector DB возвращает top-k похожих векторов.

### 3) **Reranker (опционально)**
Cross-encoder модель или LLM сортирует найденные результаты по релевантности.  
Позволяет улучшить итоговое качество за счёт более дорогой, но точной проверки.

### 4) **LLM-генерация**
LLM получает:
- запрос,
- список релевантных фрагментов,
- шаблон prompt,

и генерирует ответ, опираясь на retrieved данные.

---

# Когда что использовать

| Сценарий | Рекомендованная стратегия |
|----------|---------------------------|
| Небольший датасет (< 1M) | HNSW, FAISS/Qdrant |
| Средний датасет (1–100M) | IVF или HNSW + фильтрация |
| Огромный датасет (100M–1B+) | IVFPQ или PQ с агрессивным сжатием |
| Высокая фактичность (юридические/медицинские данные) | HNSW + reranker |
| Жёсткое ограничение по памяти | PQ |

